query GetWeather($city: String!) {
  getWeather(city: $city) {
    temperature
    description
    humidity
    windSpeed
  }
}

query GetForecast($city: String!) {
  getForecast(city: $city) {
    date
    temperature
    description
  }
}
